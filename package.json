{
  "name": "nutrition-app",
  "version": "1.0.0",
  "description": "Nutrition and body composition tracking API",
  "private": true,
  "workspaces": [
    "backend",
    "packages/types"
  ],
  "scripts": {
    "dev": "cd backend && npm run dev",
    "build": "cd backend && npm run build",
    "start": "cd backend && node dist/index.js",
    "test": "cd backend && npm test"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
