---
phase: 01-setup
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/tsconfig.eslint.json
  - backend/.env.example
  - backend/src/index.ts
  - backend/src/server.ts
  - backend/src/routes/dashboard.ts
  - backend/src/types/express.d.ts
  - backend/src/__tests__/dashboard.test.ts
  - backend/jest.config.js
  - backend/.eslintrc.js
autonomous: true
requirements:
  - REQ-02
  - REQ-04
  - REQ-05
user_setup: []

must_haves:
  truths:
    - "GET /api/dashboard returns 200 with valid JSON matching the DashboardData contract"
    - "Backend server starts without errors with npm run dev:server"
    - "Backend tests pass with npm run test:backend"
    - "CORS is configured for frontend origin in development"
  artifacts:
    - path: "backend/src/server.ts"
      provides: "Express server with middleware and routing"
      contains: "const app = express()"
    - path: "backend/src/routes/dashboard.ts"
      provides: "Dashboard API endpoint"
      contains: "router.get('/api/dashboard'"
    - path: "backend/src/__tests__/dashboard.test.ts"
      provides: "Backend endpoint test"
      contains: "describe('GET /api/dashboard'"
  key_links:
    - from: "backend/src/routes/dashboard.ts"
      to: "packages/types/src/dashboard.ts"
      via: "type import"
      pattern: "from '@nutrition/types'"
    - from: "backend/src/server.ts"
      to: "backend/src/routes/dashboard.ts"
      via: "router import"
      pattern: "import.*dashboardRouter"
---

<objective>
Build the Express.js backend API with a single `/api/dashboard` endpoint that returns mock dashboard data conforming to the shared `DashboardData` type. Establish server configuration, error handling, and a foundational test.

Purpose: Provides the backend foundation for the nutrition app and serves mock data to the frontend dashboard.
Output: Running Express API with a tested dashboard endpoint, typed using shared interfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Bootstrap context for types
The shared types package exports `DashboardData`, `DashboardMacros`, and `MacroTarget`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend scaffolding and server setup</name>
  <files>backend/package.json, backend/tsconfig.json, backend/tsconfig.eslint.json, backend/.env.example, backend/src/server.ts, backend/src/types/express.d.ts, backend/.eslintrc.js</files>
  <read_first>packages/types/src/dashboard.ts</read_first>
  <action>
    Create `backend/package.json` with name "@nutrition/backend", depend on "express", "cors", "dotenv" as dependencies, and "typescript", "@types/express", "@types/cors", "ts-node", "nodemon" as devDependencies. Add scripts: "dev:server" runs `nodemon src/index.ts`, "build" runs `tsc`. The package should depend on the local `@nutrition/types` package.

    Create `backend/tsconfig.json` extending the root tsconfig, configuring `outDir` as "dist", and managing include paths. Ensure `tsconfig.json` includes `src` and the build output goes into a `dist` directory.

    Create an environment file template `backend/.env.example` with `PORT=3001` and `FRONTEND_URL=http://localhost:3000`.

    Create `backend/src/server.ts` creating an Express app. Configure CORS to allow the `FRONTEND_URL`. Add a simple error handling middleware that catches errors and returns a JSON 500 response. Export the configured `app`.
  </action>
  <verify>
    <automated>cd backend && npm install && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - backend/package.json contains "express" and "cors" in dependencies
    - backend/tsconfig.json contains "outDir": "dist"
    - backend/src/server.ts contains "const app = express()"
    - backend/.env.example contains "PORT=3001"
    - TypeScript compilation passes in backend directory
  </acceptance_criteria>
  <done>Backend project scaffolded with Express, TypeScript, and environment configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dashboard API endpoint and index entry</name>
  <files>backend/src/routes/dashboard.ts, backend/src/index.ts, backend/src/__tests__/dashboard.test.ts, backend/jest.config.js</files>
  <read_first>backend/src/server.ts, packages/types/src/dashboard.ts</read_first>
  <action>
    Create `backend/src/routes/dashboard.ts`. Define an Express router with `GET /api/dashboard` that returns a mock `DashboardData` object. Use the exact mock data from the PLAN.md:
    - calories: { target: 2200, consumed: 1500, remaining: 700 }
    - macros: protein {150, 90, 60}, carbs {250, 180, 70}, fat {70, 50, 20}
    - expenditureEstimate: 2500
    - weightTrend: placeholder array [70.5, 70.2, 70.0, 69.8, 69.9, 70.1, 70.0]
    - weeklyAdherence: 0.85
    - goalProgress: 0.6

    Import and type the response using the shared `DashboardData` interface from `@nutrition/types`.

    Create `backend/src/index.ts` that imports the `app` from `server.ts`, reads the `PORT` from dotenv (with a default of 3001), and starts the HTTP server. Create `backend/jest.config.js` for TypeScript tests.

    Create `backend/src/__tests__/dashboard.test.ts` using Jest and Supertest. Test that `GET /api/dashboard` returns 200 and that the response body contains the expected `calories` object with `target`, `consumed`, and `remaining` properties.
  </action>
  <verify>
    <automated>cd backend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/routes/dashboard.ts contains "router.get('/api/dashboard'"
    - backend/src/routes/dashboard.ts imports from "@nutrition/types"
    - backend/src/__tests__/dashboard.test.ts contains "describe('GET /api/dashboard'"
    - `npm test` in backend/ directory passes
  </acceptance_criteria>
  <done>Dashboard endpoint returns correct mock data and the test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Untrusted input crosses from frontend to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | CORS | mitigate | CORS configured to allow only specific `FRONTEND_URL`. No wildcard `*` in production. |
| T-02-02 | DoS | Express | mitigate | Simple rate limiting can be added later (not a blocker for Phase 1). |
| T-02-03 | Spoofing | `.env` | mitigate | `.env.example` provided; secrets are not committed. |
| T-02-SC | Tampering | npm installs | mitigate | Use established packages (express, cors, dotenv, jest, supertest) only. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend && npm install` succeeds
- [ ] `cd backend && npx tsc --noEmit` passes
- [ ] `cd backend && npm test` passes
- [ ] `cd backend && npm run dev:server` starts the server on PORT 3001
</verification>

<success_criteria>
- All tasks completed
- Backend compiles without TypeScript errors
- GET /api/dashboard returns 200 and valid JSON matching the DashboardData type
- Backend test suite passes
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup/01-02-SUMMARY.md`
</output>
