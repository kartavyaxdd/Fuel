---
phase: 01-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - tsconfig.json
  - packages/types/package.json
  - packages/types/tsconfig.json
  - packages/types/src/index.ts
  - packages/types/src/dashboard.ts
  - .prettierrc
  - .eslintrc.js
  - .vscode/settings.json
autonomous: true
requirements:
  - REQ-01
  - REQ-04
  - REQ-05
  - REQ-06
user_setup: []

must_haves:
  truths:
    - "Project builds from clean checkout with single npm install && npm run dev"
    - "Shared types package is importable by both frontend and backend"
    - "Root workspace scripts run without errors"
    - "ESLint and Prettier are configured and enforced"
  artifacts:
    - path: "package.json"
      provides: "Workspace root with npm workspaces"
      contains: '"workspaces":'
    - path: "packages/types/src/index.ts"
      provides: "Shared TypeScript types"
      contains: "export interface"
    - path: "packages/types/src/dashboard.ts"
      provides: "Dashboard data contract types"
      contains: "export interface DashboardData"
  key_links:
    - from: "package.json"
      to: "packages/types"
      via: "npm workspaces"
      pattern: '"packages/types"'
    - from: "packages/types/src/dashboard.ts"
      to: "packages/types/src/index.ts"
      via: "re-export"
      pattern: "export.*from.*dashboard"
---

<objective>
Scaffold the monorepo workspace with shared TypeScript types, ESLint, Prettier, and VS Code settings. This is the foundation upon which the backend and frontend will be built.

Purpose: Establishes a clean, consistent development environment with a single source of truth for shared types.
Output: Root package.json with npm workspaces, shared types package, and unified dev tooling configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root workspace and shared types package</name>
  <files>package.json, .gitignore, packages/types/package.json, packages/types/tsconfig.json, packages/types/src/index.ts, packages/types/src/dashboard.ts</files>
  <read_first></read_first>
  <action>
    Create a monorepo root package.json with npm workspaces configured for "frontend", "backend", and "packages/*". Set the "dev" script to run both frontend and backend concurrently (using `concurrently` or native). Create a `.gitignore` for Node.js and IDE files.

    In `packages/types/`, create a dedicated TypeScript package with its own tsconfig.json and package.json (name: "@nutrition/types"). This package will house all shared domain types to prevent type duplication across frontend and backend.

    Create `packages/types/src/dashboard.ts` with these exported interfaces (matching the mock data contract):
    - `MacroTarget`: { target: number, consumed: number, Largest remaining: number }
    - `DashboardMacros`: { protein: MacroTarget, carbs: MacroTarget, fat: MacroTarget }
    - `DashboardData`: { calories: MacroTarget, macros: DashboardMacros, expenditureEstimate: number, weightTrend: number[], weeklyAdherence: number, goalProgress: number }

    In `packages/types/src/index.ts`, re-export everything from `dashboard.ts`. Ensure the package.json has "main" and "types" pointing to the correct output and "tsconfig.json" extends the root tsconfig.
  </action>
  <verify>
    <automated>npm install && npx tsc --noEmit -p packages/types/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "workspaces": ["frontend", "backend", "packages/*"]
    - packages/types/src/dashboard.ts contains "export interface DashboardData"
    - packages/types/src/index.ts contains "export * from './dashboard'"
    - npm install completes without errors
    - TypeScript compilation of packages/types passes
  </acceptance_criteria>
  <done>Shared types package compiles and exposes DashboardData, DashboardMacros, and MacroTarget interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Configure ESLint, Prettier, and VS Code settings</name>
  <files>.eslintrc.js, .prettierrc, .vscode/settings.json, tsconfig.json</files>
  <read_first>packages/types/tsconfig.json</read_first>
  <action>
    Create a root `.eslintrc.js` that extends `@typescript-eslint/recommended` and sets the parser to `@typescript-eslint/parser`. Configure Prettier via `.prettierrc` with single quotes, 2-space tabs, and a 100-character print width to enforce a consistent code style. Create `.vscode/settings.json` to enable automatic formatting on save and configure the editor to use the workspace ESLint rules.

    Create a root `tsconfig.json` that serves as the base configuration for the monorepo. It should set the target to "ES2020", module to "commonjs", and strict mode to true. It should also include all packages and apps in the "include" array, excluding node_modules and build output. This will be the source of truth that other `tsconfig.json` files extend.
  </action>
  <verify>
    <automated>npx eslint . --ext .ts,.tsx</automated>
  </verify>
  <acceptance_criteria>
    - .eslintrc.js contains "@typescript-eslint/recommended"
    - .prettierrc contains "singleQuote": true
    - .vscode/settings.json contains "editor.formatOnSave": true
    - npx eslint . runs without crashing
  </acceptance_criteria>
  <done>ESLint and Prettier are configured and run without errors on the shared types package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling | No trust boundary crossed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Use only well-known packages (TypeScript, ESLint, Prettier) from official registries. Human verification not needed for these established packages. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm install` from root succeeds
- [ ] `npx tsc --noEmit -p packages/types/tsconfig.json` passes
- [ ] `npx eslint packages/types/src --ext .ts` runs without errors
- [ ] `npx prettier --check packages/types/src` runs without errors
</verification>

<success_criteria>
- All tasks completed
- Root workspace is functional with npm workspaces
- Shared types package compiles and exports DashboardData interfaces
- ESLint, Prettier, and TypeScript are configured globally
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup/01-01-SUMMARY.md`
</output>
